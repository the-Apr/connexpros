import { createStore } from 'vuex'
import formValid from './formValid';

export default createStore({

  modules: {
    formValid,
  },

  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
})
